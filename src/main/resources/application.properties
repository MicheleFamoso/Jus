spring.application.name=Jus
spring.config.import=file:env.properties

#spring.datasource.url=jdbc:postgresql://localhost:5432/jus
#spring.datasource.username=postgres
#spring.datasource.password=${postgresql.password}
#spring.datasource.driver-class-name=org.postgresql.Driver

# Connessione MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/jus?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=${mysql.username}
spring.datasource.password=${mysql.password}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

jwt.duration=${jwt.duration}
jwt.secret=${jwt.secret}